/** Custom DataTransfer type for month picks on Category Insights (compare slots). */
export const CI_MONTH_DRAG_MIME = "application/x-ci-month";

/** Year (YYYY) picks for year-vs-year compare. */
export const CI_YEAR_DRAG_MIME = "application/x-ci-year";
